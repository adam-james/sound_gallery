import React from 'react';
import { Provider } from 'react-redux';

const Root = React.createClass({
  render: function() {
    return (
      <h1>{'IN PRODUCTION'}</h1>
    );
  }
});

export default Root;
